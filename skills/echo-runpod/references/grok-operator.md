# Grok RunPod Operator

Native Grok CLI 0.2.51 agent definition:

`C:\Users\bobmc\.grok\agents\echo-runpod-operator.md`

Discovered from `~/.grok/agents/*.md` (user) or `.grok/agents/*.md` (project). Launch:

```text
grok agent --agent-profile C:\Users\bobmc\.grok\agents\echo-runpod-operator.md stdio
```

Or open `/config-agents` and select **echo-runpod-operator**.

Default permission_mode is observe/plan. It is not a generic lieutenant. Ownership: RunPod observation, diagnosis, training preparation, cost and checkpoint monitoring, approved automation.

Cloud Grok Bot (docs.x.ai) is a different product and cannot be registered from this control plane.
