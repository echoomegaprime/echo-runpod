# runpod-pods thin router

This skill is a compatibility router. Canonical policy lives in `echo-runpod`.

Use `echo-runpod` for all new work. This file remains so existing Codex prompts that mention `runpod-pods` still resolve.
